ToDo:
- propExc, add 90 and 0deg
- check thorougly, especially scan direction

For later:
- calcAngles
- calcBrilloSpec 
(- saveHist or other stuff in subfolder)
- make final images

LapPC (cmd + pip?)
cd C:\temp\snakemake -> go to folder
snakemakeEnv\Scripts\activate -> activate environment
snakemake --cores 1 -> start file (snakemake -s Snakefile_01_02 --cores 1)

snakemake -s SnakefileAP --cores 1


Laptop miniforge
cd C:\Users\goerlitz\Documents\BrilloSnakeMake
mamba activate snakemakeEnv


visalization:
snakemake --cores 1 --dag mermaid-js > dag.md


issues Francesco: 
- temp() not working properly but if using snakemake --delete-temp-outputs it delets it -> hopefully just Windows accessing temp() file issue
- print out somehow separate/ isolated .eg. print(f"----- psf sys generated idx={i} -----", flush=True) 
- visualization of workflow (DAG) via mermaid or alternatives?

issues General:
- some wierd variables in varbiablemanager. These are from loaded libraries and should be in the Background -> Spyder consol: %reset -f

#functions taking forever1:
from scipy.ndimage import zoom
scatVol = zoom(scatVol, scale_factors, order=1)

from numpy import rot90
from scipy.ndimage import rotate